/* main function is always the first code that runs 
    in every executable rust program */
fn main() {
    println!("hello, world");   // prints text to the screen
    /* using a ! means that you're calling a macro 
        instead of a normal function */
}